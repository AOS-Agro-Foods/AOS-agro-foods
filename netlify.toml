[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — required for React Router deep links (/about, /contact, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
